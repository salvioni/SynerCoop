PORT=4000
JWT_SECRET=finanalyze-jwt-secret-troque-em-producao-use-valor-aleatorio-longo
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# ── Provedor de IA (extração de PDF e geração de relatório) ──
# Defina UMA das chaves abaixo. Se ambas existirem, Gemini é usado por padrão.
# Para forçar: LLM_PROVIDER=claude ou LLM_PROVIDER=gemini

# Google Gemini (GRÁTIS — 15 req/min, 1M tokens/dia)
# Crie em: https://aistudio.google.com/apikey
GOOGLE_AI_API_KEY=

# Anthropic Claude (pago — ~US$0.03 por análise)
ANTHROPIC_API_KEY=sk-ant-coloque-aqui

# E-mail (Gmail SMTP) — sem EMAIL_USER/EMAIL_PASS envia o link de convite no console
EMAIL_USER=salvi.pilo@gmail.com
EMAIL_PASS=pxmuzmcjeqozyuiq

# Banco de dados — sem isso usa SQLite local (data.db)
# DATABASE_URL=postgresql://usuario:senha@host:5432/finanalyze
